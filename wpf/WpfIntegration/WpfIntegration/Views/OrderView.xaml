<Grid x:Class="WpfIntegration.Views.OrderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024">
    <Grid.RowDefinitions>
        <RowDefinition Height="1*"/>
        <RowDefinition Height="10*"/>
    </Grid.RowDefinitions>
    <Button Grid.Column="0" Grid.Row="0"  Content="Back" Width="65" FontSize="16" Command="{Binding BackCommand}" HorizontalAlignment="Left" Margin="10,5,0,5"/>
    <Label Grid.Row="0" Content="Order Summary" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    <TextBlock Grid.Row="1" Text="{Binding OrderSummary}" FontSize="14" Margin="0,10,0,0"/>
</Grid>
